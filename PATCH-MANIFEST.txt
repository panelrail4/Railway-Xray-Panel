Railway-XPanel v1.1.2 -> v1.1.3 CLEAN PATCH

Fixes empty user links and the subscription error:
"No enabled inbound can produce a Railway-compatible link."

The patch makes public-domain resolution fall back to Railway forwarded Host / X-Forwarded-Host when RAILWAY_PUBLIC_DOMAIN is not visible to the backend process. User links, QR links, and subscription creation now pass the incoming request host through the resolver.

Extract directly into the v1.1.2 project root and replace existing files.

Changed files:
- backend/app/railway_domain.py
- backend/app/api/subscriptions.py
- backend/app/api/qr.py
