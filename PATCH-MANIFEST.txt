Railway-XPanel v1.1.4 -> v1.1.5 CLEAN PATCH

Fixes FastAPI startup failure caused by Request | None in QR route.
Only backend/app/api/qr.py is changed.
Extract directly over v1.1.4 and replace the file.
