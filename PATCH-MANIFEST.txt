Railway-XPanel v1.1.3 -> v1.1.4 CLEAN PATCH

Apply directly to the root of a v1.1.3 project and overwrite existing files.
This patch contains only files changed in v1.1.4.

Main fix:
- FastAPI startup crash caused by Request | None annotations on non-route helper
  functions in subscriptions.py.
- Request is now injected only by route handlers and passed to helper functions.

Files:
- CHANGELOG.md
- VERSION
- backend/app/api/qr.py
- backend/app/api/subscriptions.py